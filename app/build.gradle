apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"

    defaultConfig {
        applicationId "com.ghostflying.locationreportenabler"
        minSdkVersion 16
        targetSdkVersion 25
        versionCode 12
        versionName "1.3.0 xposed"
    }
    buildTypes {
    }
}

dependencies {
    provided fileTree(dir: 'libs', include: ['*.jar'])
}
